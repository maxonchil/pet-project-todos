import { TimeFormat12hPipe } from './time-format12h.pipe';

describe('TimeFormat12hPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFormat12hPipe();
    expect(pipe).toBeTruthy();
  });
});
